const { Router } = require('express');
const router = Router();

router.post('/', ) // funcao de criar

// funcao de editar
router.put('/:id', );

// funcao de deletar
router.delete('/:id', );

// buscar unico
router.get('/:id', );

// buscar todos


router.get('/', );

module.exports = router;